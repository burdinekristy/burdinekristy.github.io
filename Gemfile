source "https://rubygems.org"

# This provides the core Jekyll engine and GitHub Pages compatibility
gem "github-pages", group: :jekyll_plugins

# This allows you to use the "remote_theme" we put in the _config.yml
gem "jekyll-remote-theme" 